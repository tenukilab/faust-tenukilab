[build]
  base = "web"
  command = "npm install --prefix netlify/functions && npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
